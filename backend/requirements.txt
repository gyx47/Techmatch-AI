# FastAPI 核心依赖
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP请求
requests==2.31.0
httpx==0.25.2

# 认证和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AI集成
openai==1.3.0

# 向量化和向量数据库
sentence-transformers>=2.3.0
chromadb>=0.4.22
torch>=2.0.0
huggingface_hub>=0.20.0,<0.23.0
numpy<2.0.0  # ChromaDB 暂不支持 NumPy 2.0，需要限制在 1.x 版本
# 注意：如果遇到 TensorFlow DLL 问题，可以尝试：
# 1. 安装 tensorflow-cpu: pip install tensorflow-cpu
# 2. 或降级 transformers: pip install transformers==4.30.0
# transformers>=4.30.0  # 某些新版本强制依赖 TensorFlow，如果遇到问题可以降级

# 数据处理
pydantic==2.5.0

# PDF处理
PyPDF2==3.0.1
# 可选：pdfplumber==0.10.3  # 如果需要更强大的PDF解析，可以安装pdfplumber

# 开发工具
pytest==7.4.3
pytest-asyncio==0.21.1
aiohttp==3.10.10
beautifulsoup4==4.12.3
rich==13.7.1
python-dotenv==1.0.0

# 评估工具
# ragas>=0.1.0
# datasets>=2.14.0